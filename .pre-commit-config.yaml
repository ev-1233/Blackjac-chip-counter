repos:
  - repo: local
    hooks:
      - id: spdx-headers
        name: Ensure SPDX Apache-2.0 headers
        entry: python3 scripts/spdx_headers.py --check
        language: system
        files: \.(py|js|css|html)$
